# --- storefront ---
NODE_ENV=development
# BFF / API base URL (used server-side only)
API_BASE_URL=http://localhost:4000
# Values genuinely safe in the browser must be prefixed NEXT_PUBLIC_
NEXT_PUBLIC_SITE_URL=http://localhost:3000
