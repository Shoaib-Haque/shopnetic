# API process environment.
NODE_ENV=development
PORT=4000

# Postgres — matches infra/docker/docker-compose.yml (host port 5433).
DATABASE_URL="postgresql://shopnetic:shopnetic@localhost:5433/shopnetic"

# Reported by /healthz and /readyz. CI/CD sets this to the build SHA.
APP_VERSION=0.0.0
