# @shopnetic/events

Domain event name constants + envelope type, shared by every context's outbox
publisher and consumers. See `plan/02-architecture.md` §4.

STUB — payload schemas (Zod, in `@shopnetic/contracts`) and pub/sub helpers land
when the message bus is wired.
