# @shopnetic/i18n

Shared locale config + `Intl` formatting helpers. The message-catalog machinery
(`next-intl`, `messages/<locale>/<ns>.json`) lives in each app; this package holds
what is truly shared.

See `plan/24-i18n-localization.md`. STUB: locale list is `['en']`; formatting
helpers are minimal.
